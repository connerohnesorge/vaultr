# Tasks

- [ ] 1. Add `Kind::Reflect` + daily job entry (claude, opus[1m], OnSuccess cleanup) in `jobs.rs`
- [ ] 2. Gate prompt on learnings ledger mtime > last record ts
- [ ] 3. Update/add unit tests (job count, reflect shape, gate)
- [ ] 4. `cargo test -p plant` green; commit + push
